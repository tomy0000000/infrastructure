# The CRDs come from this release's presync hook, not from the chart, so a
# version bump moves both.
crds:
  enabled: false

deployment:
  envoyGateway:
    resources:
      requests:
        cpu: 50m
        memory: 128Mi
      limits:
        memory: 512Mi
